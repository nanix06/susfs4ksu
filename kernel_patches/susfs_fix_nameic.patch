--- a/fs/namei.c
+++ b/fs/namei.c
@@ -3870,6 +3870,8 @@
 {
 	struct path path;
 	int error;
+	int old_dfd = dfd;
+	struct filename *fake_filename = NULL;
 
 	if (flags & O_CREAT)
 		mode |= S_IALLUGO;
