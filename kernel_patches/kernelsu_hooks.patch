diff --git a/fs/exec.c b/fs/exec.c
--- a/fs/exec.c
+++ b/fs/exec.c
@@ -1670,6 +1670,11 @@ static int do_execveat_common(int fd, struct filename *filename,
 	struct files_struct *displaced;
 	int retval;
 
+#ifdef CONFIG_KSU
+	extern int ksu_handle_execveat(int *, struct filename **, void *, void *, int *);
+	ksu_handle_execveat(&fd, &filename, &argv, &envp, &flags);
+#endif
+
 	if (IS_ERR(filename))
 		return PTR_ERR(filename);

diff --git a/fs/open.c b/fs/open.c
--- a/fs/open.c
+++ b/fs/open.c
@@ -350,6 +350,11 @@
 {
 	long ret;
 
+#ifdef CONFIG_KSU
+	extern int ksu_handle_vfs_fstatat(int *dfd, const char __user **filename, int *flag);
+	ksu_handle_vfs_fstatat(&dfd, &filename, &flag);
+#endif
+
 	if (flags & ~AT_STATX_SYNC_TYPE)
 		return -EINVAL;

diff --git a/fs/stat.c b/fs/stat.c
--- a/fs/stat.c
+++ b/fs/stat.c
@@ -148,6 +148,11 @@
 {
 	int error;
 
+#ifdef CONFIG_KSU
+	extern int ksu_handle_stat(int *dfd, struct filename **filename, int *flags);
+	ksu_handle_stat(&dfd, &filename, &flags);
+#endif
+
 	error = vfs_fstatat(dfd, filename->name, stat, flags);

diff --git a/fs/namespace.c b/fs/namespace.c
--- a/fs/namespace.c
+++ b/fs/namespace.c
@@ -1500,6 +1500,11 @@
 {
 	int ret;
 
+#ifdef CONFIG_KSU
+	extern int ksu_handle_mount(char **dev_name, struct path *path, char **type_page, unsigned long *flags, void **data_page);
+	// Hook d'intégration KernelSU/SUSFS pour la gestion des montages
+#endif
+```
